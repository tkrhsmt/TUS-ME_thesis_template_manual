---
title: comment
description: 添削用コメント枠を出力する
---

## 構文

```typst
#comment(title: none)[内容]
```

## 説明

灰色のコメント枠を出力します。`title` は任意で、指定すると枠上部に題目を表示します。`check-contents` を有効にすると、題目（未指定なら「無題」）が文末のコメント一覧に収集されます。

```typst
#comment(title: [確認事項])[この節を確認してください。]
```
