---
title: signary-list
description: 記号表の区分を出力する
---

## 構文

```typst
#signary-list(title: [], body)
```

## 説明

`title` を表示後、左列幅 10em、罫線なしの 2 列相当の `table` に `body` を展開します。段落字下げを無効にするため、記号表の Alphabet・Greek などの区分に使います。
