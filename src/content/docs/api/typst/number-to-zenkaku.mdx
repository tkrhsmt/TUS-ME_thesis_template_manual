---
title: number-to-zenkaku
description: 数字を全角数字に変換する
---

## 構文

```typst
#number-to-zenkaku(num, nonum: false)
```

## 説明

`num` 中の半角・全角数字を全角数字にして返します。`nonum: true` では数字以外を `＊` に変換します。`thesis_title` が年度と学籍番号の表記に内部利用します。
