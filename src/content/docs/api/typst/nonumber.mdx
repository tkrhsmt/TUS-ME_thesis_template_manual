---
title: nonumber
description: equate の式番号を無効にするラベル
---

## 構文

```typst
#align(... ) <nonumber>
```

## 説明

`nonumber` は `<equate:revoke>` を値に持つテンプレート定数です。`equate` パッケージによる整列数式の番号付けを抑止するラベルとして使います。
